{
  "name": "education-il",
  "version": "0.2.0",
  "license": "MIT",
  "type": "module",
  "scripts": {
    "test": "node --test functions/chatPolicy.test.mjs",
    "test:cloud": "node scripts/test-chat-cloud.mjs",
    "dev": "vite",
    "start": "node scripts/dev.mjs",
    "build": "vite build",
    "preview": "vite preview",
    "generate:assets": "node scripts/generate-assets.js",
    "prebuild": "npm run generate:assets",
    "deploy:hosting": "npm run build && firebase deploy --only hosting",
    "deploy": "npm run build && firebase deploy",
    "release": "npm version minor --no-git-tag-version && npm run build && firebase deploy"
  },
  "dependencies": {
    "firebase": "^12.18.0",
    "react": "^18.3.1",
    "react-dom": "^18.3.1",
    "react-markdown": "^10.1.0",
    "react-router-dom": "^7.18.3",
    "rehype-raw": "^7.0.0",
    "remark-gfm": "^4.0.1"
  },
  "devDependencies": {
    "@vitejs/plugin-react": "^4.3.4",
    "playwright": "^1.63.0",
    "sharp": "^0.33.5",
    "vite": "^5.4.11"
  },
  "allowScripts": {
    "sharp@0.33.5": true,
    "esbuild@0.21.5": true
  }
}
